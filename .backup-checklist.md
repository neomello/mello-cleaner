# 📋 Checklist de Proteção - Arquivos Importantes

## ⚠️ ANTES DE QUALQUER MODIFICAÇÃO IMPORTANTE

### 1. Backup e Versionamento
- [ ] Fazer commit das mudanças atuais
- [ ] Criar branch para experimentos (`git checkout -b experiment/nome`)
- [ ] Verificar status do git (`git status`)
- [ ] Verificar se há arquivos não commitados importantes

### 2. Arquivos Críticos que NUNCA devem ser perdidos

#### Configuração e Ambiente
- [ ] `.env` e variantes (`.env.local`, `.env.production`)
- [ ] Arquivos de configuração (`config.json`, `settings.yaml`)
- [ ] Scripts de setup (`setup.sh`, `install.sh`)

#### Blockchain e Web3
- [ ] Contratos Solidity (`*.sol`)
- [ ] Scripts de deploy (`deploy.js`, `deploy.ts`)
- [ ] Configurações de rede (`hardhat.config.js`, `truffle-config.js`)
- [ ] Arquivos de ABI (`artifacts/`, `build/contracts/`)

#### Chaves e Credenciais (NUNCA no Git)
- [ ] Chaves privadas (`.key`, `.pem`)
- [ ] Mnemônicos e seed phrases
- [ ] Arquivos de keystore
- [ ] Tokens de API

#### Código e Documentação
- [ ] Arquivos principais do projeto
- [ ] Documentação (`README.md`, `docs/`)
- [ ] Scripts utilitários (`tools/`, `utils/`)
- [ ] Testes (`tests/`)

### 3. Verificações de Segurança

#### Antes de Commitar
- [ ] Verificar se há credenciais hardcoded (`grep -r "password\|secret\|key" --include="*.py" --include="*.js"`)
- [ ] Verificar se `.env` está no `.gitignore`
- [ ] Verificar se chaves privadas não estão sendo commitadas
- [ ] Revisar arquivos modificados (`git diff`)

#### Antes de Deletar Arquivos
- [ ] Verificar se o arquivo está versionado (`git ls-files arquivo`)
- [ ] Fazer backup antes de deletar
- [ ] Verificar dependências do arquivo
- [ ] Confirmar que não é arquivo crítico

### 4. Comandos Úteis de Recuperação

```bash
# Ver histórico de arquivos deletados
git log --all --full-history --diff-filter=D --summary

# Recuperar arquivo deletado
git checkout HEAD~1 -- caminho/do/arquivo

# Ver mudanças recentes
git log --oneline --since="2 days ago"

# Ver reflog (histórico completo incluindo operações)
git reflog

# Recuperar commit perdido
git reflog
git checkout <commit-hash>
```

### 5. Padrões de Arquivos Importantes

#### Arquivos que DEVEM estar no Git
- ✅ Código fonte (`.py`, `.js`, `.sol`, `.ts`)
- ✅ Configurações de exemplo (`.env.example`, `config.example.json`)
- ✅ Documentação (`README.md`, `docs/`)
- ✅ Scripts de build e deploy (sem credenciais)
- ✅ Testes (`tests/`, `*.test.js`)

#### Arquivos que NUNCA devem estar no Git
- ❌ Credenciais (`.env`, `.key`, `.secret`)
- ❌ Chaves privadas e mnemonics
- ❌ Arquivos de build grandes (`node_modules/`, `dist/`)
- ❌ Arquivos temporários (`*.log`, `*.tmp`)
- ❌ Arquivos de sistema (`.DS_Store`)

## 🔒 Proteções Ativas

- ✅ `.gitignore` configurado para proteger arquivos sensíveis
- ✅ `.cursorignore` configurado para não indexar arquivos grandes
- ✅ `.cursorrules` com regras de proteção NEØ
- ✅ Checklist de backup antes de modificações

## 📝 Notas Importantes

- **Sempre** faça commit antes de grandes mudanças
- **Nunca** commite credenciais ou chaves privadas
- **Sempre** use variáveis de ambiente para configurações sensíveis
- **Sempre** verifique o histórico antes de deletar arquivos importantes
- **Sempre** mantenha backups de arquivos críticos fora do repositório

